# Use the same PostgreSQL database as the Python services.
# Node accepts a standard PostgreSQL URL (not postgresql+asyncpg).
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/onfood
PORT=8002
